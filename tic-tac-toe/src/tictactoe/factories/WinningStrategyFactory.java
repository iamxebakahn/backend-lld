package tictactoe.factories;

import tictactoe.models.WinningStrategyType;
import tictactoe.strategies.WinningStrategy;

public class WinningStrategyFactory {
    public static WinningStrategy getWinningStrategy(WinningStrategyType winningStrategyType){
       return  null;

        /* Complete it - in sometime
        Once concrete winning strategy classes are ready complete code to return correct winning strategy object
        if(winningStrategyType == WinningStrategyType.CORNERS){}

            else if(winningStrategyType == WinningStrategyType.COLUMN){}

                else {} */

    }
}
